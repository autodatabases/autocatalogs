import GetManufacturersUsecase from '../../src/usecases/GetManufacturersUsecase.mjs';
import { createScope } from '../../core.js';

test('Get manufacturers (count)', async () => {
// 	const scope = await createScope();
// 	const instance = new GetManufacturersUsecase(scope.cradle);
// 	const result = await instance.process({
// 		count: 8
// 	});

// 	expect(result).toHaveLength(8);
// });

// test('Get manufacturers (filter)', async () => {
// 	const scope = await createScope();
// 	const instance = new GetManufacturersUsecase(scope.cradle);
// 	const result = await instance.process({
// 		query: 'bu'
// 	});

// 	for (const row of result) {
// 		expect(row.name).toMatch(/bu/i);
// 	}
});
